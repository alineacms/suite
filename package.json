{
  "version": "0.1.0",
  "name": "@benmerckx/suite",
  "module": "suite.js",
  "type": "module",
  "files": [
    "suite.js",
    "suite.d.ts"
  ],
  "devDependencies": {
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^5.0.0"
  },
  "scripts": {
    "test": "node --test && deno test && bun test",
    "deploy": "npm publish && npx jsr publish"
  },
  "publishConfig": {
    "access": "public"
  }
}